# Webapp Scripts

Launcher scripts for `repos/mir-webapp`.

## Command

```bash
./scripts/webapp/run.sh
```

The webapp is not implemented yet. The launcher currently exits with
`TBD: still not developed`.
