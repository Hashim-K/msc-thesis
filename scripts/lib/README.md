# Script Library

Shared shell helpers used by scripts in sibling folders.

## Files

| File | Purpose |
|------|---------|
| `env.sh` | Load `.env` and `.env.local` without shell-sourcing arbitrary values |

Do not call files in this folder directly.
